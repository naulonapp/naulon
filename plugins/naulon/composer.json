{
	"name": "naulon/naulon-wp",
	"description": "Charge AI agents for reading your articles. Humans always read free.",
	"type": "wordpress-plugin",
	"license": "GPL-2.0-or-later",
	"require": {
		"php": ">=7.4"
	},
	"require-dev": {
		"phpunit/phpunit": "^9",
		"yoast/phpunit-polyfills": "^4.0"
	},
	"scripts": {
		"test": "phpunit",
		"test:integration": "phpunit -c phpunit-integration.xml.dist",
		"lint": "find . -path ./vendor -prune -o -name '*.php' -print -exec php -l {} \\;"
	},
	"scripts-descriptions": {
		"test": "The unit suite: the decision functions, no WordPress, a tenth of a second.",
		"test:integration": "The integration suite: needs WordPress and MySQL. Run bin/install-wp-tests.sh first.",
		"lint": "Syntax-check every PHP file."
	}
}
