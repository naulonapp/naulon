/** Dashboard tunables — named so a contributor changes behaviour in one place. */

/** How often the SSE loop re-reads the ledger for newly-settled crossings (ms). */
export const POLL_MS = 1000;

/** How many recent crossings the feed carries. */
export const RECENT_LIMIT = 12;
