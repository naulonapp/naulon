# Contributor override: build both services from this working tree instead of pulling
# the published image. `image:` is kept so the built layers land under the same name.
#
#   docker compose -f docker-compose.yml -f docker-compose.build.yml up --build
services:
  tollgate:
    build: .
  dashboard:
    build: .
